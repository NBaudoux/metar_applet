{
    "AD_ICAO": {
        "type" : "entry",
        "default": "EKRK",
        "description": "AD's ICAO you want to get the METAR from"
    },
    "LINE_LENGTH": {
        "type" : "scale",
        "default": 50,
        "min": 20,
        "max": 60,
        "step": 1,
        "description": "Length of the METAR's line"
    },
    "TWO_LINES": {
        "type": "checkbox", 
        "default": "true",
        "description": "Show the METAR on 2 lines if too long"
    }
}